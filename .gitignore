# ── OS ──────────────────────────────────────────────
.DS_Store

# ── Node ────────────────────────────────────────────
node_modules/
**/node_modules/
npm-debug.log*

# ── Web build output (regenerated by CI / make-web.sh) ─
vocab-study-app/dist/

# ── Dev / OCR scratch ───────────────────────────────
vocab-study-app/.playwright-cli/
vocab-study-app/output/
tmp/

# ── Windows packaging output (regenerate: tools/package-windows.sh) ─
dist-windows/
*.zip

# ── Large data not needed at runtime (regenerate via tools/) ─
tools/etymology/
vocab-study-app/public/data/vocab.tesseract-backup.json

# ── Copyrighted exam syllabus PDF (OCR source, not redistributed) ─
*.pdf

# ── Cloze/reading generation intermediates (regenerable; final data is in public/data) ─
vocab-study-app/.cloze-gen/
vocab-study-app/.reading-gen/
